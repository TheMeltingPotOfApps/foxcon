# Node Environment
NODE_ENV=development

# Server Configuration
PORT=5002
HOST=0.0.0.0
FRONTEND_URL=http://localhost:5001
BACKEND_URL=http://localhost:5002
API_BASE_URL=http://localhost:5002
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5001,http://127.0.0.1:3000,http://127.0.0.1:5001

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=sms_user
DB_PASSWORD=sms_password
DB_DATABASE=sms_platform
DB_SSL=false
DB_MAX_CONNECTIONS=30
DB_MIN_CONNECTIONS=2

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-change-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-jwt-refresh-secret-key-change-in-production
JWT_REFRESH_EXPIRES_IN=7d

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# RabbitMQ Configuration
RABBITMQ_URL=amqp://sms_user:sms_password@localhost:5672

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Stripe Product IDs
STRIPE_PRODUCT_FREE=prod_your_free_product_id
STRIPE_PRODUCT_STARTER=prod_your_starter_product_id
STRIPE_PRODUCT_PROFESSIONAL=prod_your_professional_product_id
STRIPE_PRODUCT_ENTERPRISE=prod_your_enterprise_product_id

# Stripe Price IDs (Monthly)
STRIPE_PRICE_FREE=price_your_free_price_id
STRIPE_PRICE_STARTER=price_your_starter_price_id
STRIPE_PRICE_PROFESSIONAL=price_your_professional_price_id
STRIPE_PRICE_ENTERPRISE=price_your_enterprise_price_id

# Stripe Meter IDs (for usage-based billing)
STRIPE_METER_SMS_SENT=mtr_your_sms_meter_id
STRIPE_METER_CALL_DURATION=mtr_your_call_duration_meter_id
STRIPE_METER_AI_MESSAGE=mtr_your_ai_message_meter_id
STRIPE_METER_AI_VOICE=mtr_your_ai_voice_meter_id

# Encryption Secret (for CRM integrations and sensitive data)
ENCRYPTION_SECRET=your-encryption-secret-key-change-in-production

# Webhook Configuration
WEBHOOK_BASE_URL=https://api.nurtureengine.net

# AI Service API Keys (stored in database via SystemConfig, but can be set here as fallback)
# These are typically managed through the system config API
ELEVENLABS_API_KEY=your_elevenlabs_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key
